# 太阳神全功能 - 逆向分析报告

> 分析日期: 2026-06-01
> 分析目标: 太阳神全功能0601.tipa
> 对比基准: Amazon2.tipa (DeltaForce_TrollKit)

---

## 1. 二进制概览

| 属性 | 太阳神 (pofi) | Amazon (DeltaForce_TrollKit) |
|------|--------------|------------------------------|
| 文件大小 | 25.4 MB | 源码项目 (~20MB .tipa) |
| 架构 | arm64 | arm64 |
| Bundle ID | `com.rn.apollo` | `com.apple.stocks` (伪装) |
| Principal Class | `Main` | AppDelegate |
| 总函数数 | 49,517 | ~500 |
| 字符串数 | 120,321 | ~1,000 |
| 入口点 | `_DecryptSpace`, `_EncryptedString` | `main()` |
| 渲染引擎 | ImGui + Metal | ImGui + Metal |
| 依赖库 | libjailbreak.dylib, libchoma.dylib | ExternalStubs (weak link) |
| iOS 目标 | iOS 13.0+ | iOS 13.0-16.x |
| 安装方式 | TrollStore persist install | TrollStore IPA |

---

## 2. 双进程架构 (核心差异)

太阳神最关键的架构设计是**双 UIApplication**模式，这是 Amazon 版本没有的：

### 2.1 MainApplication
- 类: `MainApplication : UIApplication`
- Delegate: `MainApplicationDelegate`
- 职责: 主界面、卡密认证、部署管理、持久化安装
- `didFinishLaunchingWithOptions` → 通过函数指针 `off_100411C88()` 间接调用

### 2.2 HUDMainApplication
- 类: `HUDMainApplication : UIApplication`
- Delegate: `HUDMainApplicationDelegate`
- 职责: 作弊覆盖层渲染、ImGui 菜单、Metal 渲染循环
- `didFinishLaunchingWithOptions` → 创建 `HUDRootViewController` → 通过函数指针 `off_1015F5E90()` 间接调用

### 2.3 双进程优势
```
┌─────────────────────────┐     ┌──────────────────────────┐
│   MainApplication       │     │   HUDMainApplication     │
│   (com.rn.apollo)       │     │   (Overlay Process)      │
│                         │     │                          │
│  ┌───────────────────┐  │     │  ┌────────────────────┐  │
│  │ RootViewController│  │     │  │HUDRootViewController│  │
│  │  - 卡密登录        │  │     │  │  - ImGui 菜单       │  │
│  │  - 主机选择        │  │     │  │  - Metal 渲染       │  │
│  │  - 部署/卸载       │  │     │  │  - ESP 覆盖层       │  │
│  │  - 服务器检查      │  │     │  │  - HID 事件回调     │  │
│  └───────────────────┘  │     │  └────────────────────┘  │
│                         │     │                          │
│  UIWindow (标准)        │     │  HUDMainWindow (特殊)    │
│                         │     │  + SBS 托管              │
└─────────────────────────┘     └──────────────────────────┘
```

进程隔离的好处:
- 覆盖层进程崩溃不影响主进程
- 游戏反作弊检测主进程时，覆盖层进程不受影响
- SBS Accessibility 托管使覆盖层在游戏上方保持可见

---

## 3. SBS 窗口托管 (关键修复点)

### 3.1 太阳神的实现

```
类名: SBSAccessibilityWindowHostingController (明文存储，无加密)
选择器: registerWindowWithContextID:atLevel:
类型编码: v@:Id (简化版)
参数:    只传 contextID (unsigned int) + level (double)
        不传 UIWindow 对象指针
调用方式: NSInvocation 动态调用
```

反编译伪代码 (sub_1000B59A4):
```objc
// 1. 创建 NSMethodSignature
NSMethodSignature *sig = [NSMethodSignature signatureWithObjCTypes:"v@:Id"];

// 2. 创建 NSInvocation
NSInvocation *inv = [NSInvocation invocationWithMethodSignature:sig];
[inv setTarget:hostingController];

// 3. 设置选择器
SEL sel = NSSelectorFromString(@"registerWindowWithContextID:atLevel:");
[inv setSelector:sel];

// 4. 只传 2 个参数 (contextID + level)
[inv setArgument:&contextId atIndex:2];  // unsigned int
[inv setArgument:&winLevel atIndex:3];   // double
[inv invoke];
```

### 3.2 Amazon 原始实现 (已修复)

Amazon 原版使用的错误方式:
```objc
// 错误的选择器名
SEL sel = NSSelectorFromString(@"registerWindow:contextID:windowLevel:");

// 错误的类型编码（全长版）
NSMethodSignature *sig = [NSMethodSignature signatureWithObjCTypes:"v32@0:8@16Q24d28"];

// 传了 3 个参数（包括 UIWindow 对象）
[inv setArgument:&window atIndex:2];     // UIWindow* (不需要!)
[inv setArgument:&contextId atIndex:3];  // unsigned int
[inv setArgument:&winLevel atIndex:4];   // double
```

### 3.3 修复方案

已将 `HUDController.m` 中的 `attachWindowToHostingController` 更新为太阳神的实现方式。

太阳神通过 contextID 识别窗口而非传递 UIWindow 对象，这符合 SBS (SpringBoard Services) 通过 context ID 管理窗口的机制。

---

## 4. HUDMainWindow 专有属性

太阳神的 `HUDMainWindow` 重写了多个 UIWindow 私有方法来增强隐身能力:

| 方法 | 返回值 | 作用 |
|------|--------|------|
| `+_isSystemWindow` | YES (类方法) | 标记为系统窗口，绕过某些检测 |
| `-_isWindowServerHostingManaged` | YES | 窗口服务器托管，提升层级 |
| `-_ignoresHitTest` | YES | 穿透触摸到下层游戏窗口 |
| `-_isSecure` | YES | 安全上下文，防止截屏检测 |
| `-_shouldCreateContextAsSecure` | YES | 创建安全渲染上下文 |
| `-initWithFrame:` | 使用 `_initWithFrame:attached:` | 非标准初始化链 |

---

## 5. XPF 内核框架

太阳神和 Amazon 共用 XPF 内核框架，函数名完全一致。以下为核心函数:

### 5.1 初始化函数
```
xpf_common_init()      - 通用内核初始化 (0x2d5c4c, 1192 bytes)
xpf_non_ppl_init()     - 非 PPL 初始化 (0x2dba6c, AMFI + TrustCache)
xpf_ppl_init()         - PPL 页保护层初始化 (0x2dc804)
xpf_start_with_kernel_path() - 完整 kernelcache 解析 (0x2ddf0c, 2336 bytes)
xpf_item_register()    - 链表注册表，分配 0x30 字节结构体
```

### 5.2 内核符号解析
```
kernelSymbol.start_first_cpu
kernelSymbol.fatal_error_fmt
kernelSymbol.arm_vm_init
kernelSymbol.phystokv          - 物理地址→内核虚拟地址转换
kernelSymbol.pmap_bootstrap
kernelSymbol.pointer_mask
kernelSymbol.task_crashinfo_release_ref
kernelSymbol.task_collect_crash_info
kernelSymbol.proc_apply_sandbox - 进程沙箱操作
kernelSymbol.pmap_tt_deallocate
kernelSymbol.ppl_enter          - PPL 入口
```

### 5.3 内核 Gadget
```
hw_lck_ticket_reserve_orig_allow_invalid_signed - 绕过签名验证
kernelGadget.hw_lck_ticket_reserve_orig_allow_invalid_signed
kernelSymbol.ml_sign_thread_state - 线程状态签名
```

### 5.4 物理内存访问 (libjailbreak.dylib)
```
physread64(phys_addr)     - 读取 64 位物理地址
physwritebuf(phys_addr, buf, size) - 写入物理内存
phystokv(phys_addr)       - 物理→内核虚拟转换
jb_init()                 - 越狱原语初始化
xpf_inject_dylib(pid, path) - dylib 注入
```

---

## 6. libchoma.dylib 代码生成

太阳神依赖 libchoma.dylib 进行运行时代码生成:

```
macho_get_filetype(path)   - 解析 Mach-O 文件类型
arm64_gen_mov_reg(reg, val) - 生成 arm64 mov 寄存器指令
```

---

## 7. 反逆向工程措施

### 7.1 函数指针间接调用 (太阳神特有)

大量关键方法通过函数指针表 (`off_*`) 间接调用，导致 IDA 反编译失败:

| 原始方法 | 实际调用 |
|----------|---------|
| `-[MainApplicationDelegate didFinishLaunching...]` | `off_100411C88()` |
| `-[RootViewController viewDidLoad]` | `off_1003F93A8()` |
| `-[RootViewController createViews]` | `off_1003F9EB8()` |
| `-[RootViewController createLoginViews]` | `off_1003FD4E8()` |
| `-[HUDMainApplicationDelegate didFinishLaunching...]` | `off_1015F5E90()` |

### 7.2 控制流混淆

使用 `BR X8/X9/X10` 无条件跳转打断基本块连续性:
```asm
ADRP X8, #off_XXXX@PAGE
LDR  X9, [X8, #off_XXXX@PAGEOFF]
BR   X9              ; 跳转到函数指针指向的实际代码
```

### 7.3 字符串加密

入口点 `_DecryptSpace` 和 `_EncryptedString` 表明存在运行时字符串解密，但 SBS 类名等关键字符串以明文存储。

### 7.4 对比表

| 保护技术 | 太阳神 | Amazon |
|---------|--------|--------|
| 函数指针表 | 重度使用 | 不使用 |
| 控制流混淆 | BR 跳转 | 无 |
| 字符串加密 | 部分 (DecryptSpace) | 部分 (NEON XOR) |
| 类名混淆 | 明文 SBS 类名 | 明文 (原版 XOR 提取失败) |
| 反调试 | 待分析 | 崩溃信号处理器 |

---

## 8. 应用功能结构

### 8.1 RootViewController 功能
```
RootViewController
├── viewDidLoad          → off_1003F93A8() [函数指针]
├── createViews          → off_1003F9EB8() [函数指针]
├── createLoginViews     → off_1003FD4E8() [函数指针]
├── startDeployFlow      - 部署/持久化安装流程
├── checkAndStartServer  - 检查并启动服务
├── hostPickerSelectButtonTapped: - 选择目标主机
├── dismissHostPicker    - 关闭主机选择器
├── showCardKeyConfirmDialog: - 卡密确认对话框
├── showKillDaemonButton - 显示终止守护进程按钮
├── reloadHostPickerList - 刷新主机列表
├── layoutHostPicker     - 布局主机选择器
├── showHostPickerWithCandidates: - 显示可选主机
├── makeDecorCircle:r:g:b:a: - UI 装饰圆
├── iconImageForSystemApp: - 获取系统应用图标
├── defaultDeployButtonTitle - 默认部署按钮标题
├── currentBundleHasInstallBackup - 检查安装备份
├── isCurrentProcessReadyToLaunchPlugin - 检查插件就绪
├── shouldShowUninstallButton - 是否显示卸载按钮
└── performUninstallForBundleID: - 执行卸载
```

### 8.2 卡密认证系统
```
UITextField *cardKeyField    - 卡密输入框
showCardKeyConfirmDialog:    - 确认对话框
```

太阳神使用卡密（激活码）系统进行授权验证，比 Amazon 的简单登录更商业化。

### 8.3 部署/持久化机制
```
startDeployFlow
__completeAndRunAsPlugin
isPersistenceInstallComplete
installedPersistenceTarget
deployButton / deployButtonTapped
uninstallButton / uninstallBtnGradient
isTrollStoreInstalled
```

具备完整的 TrollStore 持久化安装流程: 部署 → 持久化安装 → 作为插件运行。

---

## 9. ImGui + Metal 渲染栈

太阳神和 Amazon 使用相同的渲染栈:

```
Metal.framework
├── MTLDevice (GPU 设备)
├── CAMetalLayer (渲染层)
├── MTLRenderPipelineDescriptor
├── MetalBuffer 类 (自定义)
└── MetalContext 类 (自定义)

Dear ImGui
├── ImGuiTableFlags_SizingFixedFit
├── ImGuiTableFlags_SizingFixedSame
├── ImGuiTableFlags_SizingStretchProp
└── ImGuiTableFlags_SizingStretchSame
```

两者都使用 60fps 的 Metal 后端的 ImGui 进行作弊菜单渲染。

---

## 10. 目标游戏

两者目标相同:
```
Bundle ID: com.tencent.tmgp.dfm
游戏名: 三角洲行动 (Delta Force)
```

太阳神中包含 83 处 "三角洲" 字符串引用（武器配置数据），包括:
- ".50BMG Full Metal Jacket"
- 各种武器名称和弹药类型

===

## 11. Amazon 需要借鉴的改进点

### 11.1 已完成修复
- [x] **SBS 方法签名**: `registerWindowWithContextID:atLevel:` 替换 `registerWindow:contextID:windowLevel:`
- [x] **类型编码**: `v@:Id` 替换 `v32@0:8@16Q24d28`
- [x] **参数数量**: 只传 contextID + level，不传 UIWindow 对象

### 11.2 建议但未实施
- [ ] **双进程架构**: 分离 MainApplication 和 HUDMainApplication
- [ ] **HUDMainWindow 安全属性**: `_isSecure`, `_shouldCreateContextAsSecure`, `_isSystemWindow`
- [ ] **函数指针间接调用**: 增加反逆向工程强度
- [ ] **_initWithFrame:attached:**: 非标准窗口初始化
- [ ] **卡密系统**: 商业化的授权验证
- [ ] **持久化部署流程**: TrollStore persist install 完整流程

### 11.3 低优先级
- [ ] 控制流混淆 (BR 跳转)
- [ ] 字符串运行时加密/解密
- [ ] 主机选择器 (支持注入多个目标)

---

## 12. 安全注意事项

1. **SBSAccessibilityWindowHostingController** 是 SpringBoard 私有类，在非越狱环境不可用
2. 卡密认证依赖远程服务器验证 (`checkAndStartServer`)
3. XPF 内核框架需要越狱或 TrollStore + CoreTrust 绕过
4. Metal 覆盖层渲染可能被游戏反作弊检测到渲染管线异常
5. 太阳神的函数指针表 (off_*) 策略对 IDA Pro 分析有效，但对 Frida 动态插桩无效

---

## 13. 文件清单

```
太阳神全功能0601.tipa (IPA 包)
├── Payload/
│   └── pofi.app/
│       ├── pofi                    - 主可执行文件 (25.4 MB, arm64)
│       ├── libjailbreak.dylib      - 越狱原语库 (708 KB)
│       ├── libchoma.dylib          - 代码生成/Mach-O 解析 (388 KB)
│       ├── Info.plist              - Bundle: com.rn.apollo
│       └── AppIcon*.png            - 应用图标
```

---

*报告结束。基于 IDA Pro 9.0 反编译分析。*
